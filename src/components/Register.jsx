export default function Register() {
  return (
    <>
      <h2>Mon composant register</h2>
      <form>
        <label>Email</label>
        <input type="text" placeholder="Email" />
        <label>Password</label>
        <input type="password" placeholder="Password" />
        <label>Vérifier votre password</label>
        <input type="password" placeholder="Vérifier le password" />
      </form>
    </>
  );
}
